...
Run ⚠️ Jenkins Credential Setup Reminder
  if: github.event.inputs.tool == 'jenkins' && github.event.inputs.action == 'install'
  run: |...
  shell: /usr/bin/bash -e {0}
********************************************************************************
⚠️  IMPORTANT: Jenkins requires manual setup for credentials and agents.
👉 After this deployment completes, run the following script locally
   while connected to the EKS cluster:

   tools/cicd/jenkins/setup-credentials.sh

********************************************************************************
...
Run Deploy Jenkins
  cd cicd/jenkins
  helm dependency update
  ...
