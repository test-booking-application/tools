name: Deploy Tools to EKS
on:
  workflow_dispatch:
    inputs:
      tool:
        description: 'Tool to deploy'
        required: true
        type: choice
        options:
          - all
          - prometheus
          - grafana
          - alertmanager
          - elasticsearch
          - kibana
          - filebeat
          - nginx-ingress
          - cloudwatch-monitoring
          - external-dns
          - jenkins  # Note: Jenkins is handled separately
      action:
        description: 'Action'
        required: true
        type: choice
        options:
          - install
          - uninstall

env:
  AWS_REGION: us-east-1
  EKS_CLUSTER_NAME: ticket-booking-eks

jobs:
  # Common setup job (runs first, provides artifacts if needed, but kept simple)
  setup:
    name: Setup Environment
    runs-on: ubuntu-latest
    outputs:
      # No outputs needed, but can add if matrix used later
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.EKS_CLUSTER_NAME }}
          kubectl get nodes
      
      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: 'latest'
      
      - name: Install eksctl
        run: |
          ARCH=amd64
          PLATFORM=$(uname -s)_$ARCH
          curl -sLO "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_$PLATFORM.tar.gz"
          tar -xzf eksctl_$PLATFORM.tar.gz -C /tmp && rm eksctl_$PLATFORM.tar.gz
          sudo mv /tmp/eksctl /usr/local/bin
          eksctl version

  # Deploy all tools EXCEPT Jenkins (runs for any tool selection, including 'all')
  deploy-non-jenkins:
    name: Deploy Non-Jenkins Tools
    runs-on: ubuntu-latest
    needs: setup
    if: ${{ always() }}  # Always run if triggered, but steps will filter
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      # Re-run AWS/kubectl/Helm here or use cache/artifacts; for simplicity, duplicate minimal setup
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.EKS_CLUSTER_NAME }}
          kubectl get nodes
      
      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: 'latest'
      
      - name: Install eksctl
        run: |
          ARCH=amd64
          PLATFORM=$(uname -s)_$ARCH
          curl -sLO "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_$PLATFORM.tar.gz"
          tar -xzf eksctl_$PLATFORM.tar.gz -C /tmp && rm eksctl_$PLATFORM.tar.gz
          sudo mv /tmp/eksctl /usr/local/bin
          eksctl version
      
      # All original steps EXCEPT Jenkins
      - name: Deploy Prometheus
        if: |
          (github.event.inputs.tool == 'prometheus' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd monitoring/prometheus
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            helm upgrade --install prometheus . -n monitoring --create-namespace -f values.yaml --wait
            echo "✅ Prometheus deployed"
          else
            helm uninstall prometheus -n monitoring || true
            echo "🗑️ Prometheus removed"
          fi
      
      - name: Deploy Grafana
        if: |
          (github.event.inputs.tool == 'grafana' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd monitoring/grafana
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            helm upgrade --install grafana . -n monitoring --create-namespace -f values.yaml --wait
            echo "✅ Grafana deployed"
            kubectl get svc -n monitoring grafana
          else
            helm uninstall grafana -n monitoring || true
            echo "🗑️ Grafana removed"
          fi
      
      - name: Deploy Alertmanager
        if: |
          (github.event.inputs.tool == 'alertmanager' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd monitoring/alertmanager
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            helm upgrade --install alertmanager . -n monitoring --create-namespace -f values.yaml --wait
            echo "✅ Alertmanager deployed"
          else
            helm uninstall alertmanager -n monitoring || true
            echo "🗑️ Alertmanager removed"
          fi
      
      - name: Deploy CloudWatch Monitoring
        if: |
          (github.event.inputs.tool == 'cloudwatch-monitoring' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            echo "🚀 Installing CloudWatch Container Insights..."
            cd monitoring/cloudwatch
            chmod +x install-addon.sh
            ./install-addon.sh
            echo "✅ CloudWatch Monitoring deployed"
          else
            echo "🗑️ Removing CloudWatch Monitoring..."
            aws eks delete-addon --cluster-name ${{ env.EKS_CLUSTER_NAME }} --addon-name amazon-cloudwatch-observability --region ${{ env.AWS_REGION }} || true
            kubectl delete namespace amazon-cloudwatch || true
            echo "✅ CloudWatch Monitoring removed"
          fi
      
      - name: Deploy Elasticsearch
        if: |
          (github.event.inputs.tool == 'elasticsearch' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd logging/elasticsearch
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            helm upgrade --install elasticsearch . -n logging --create-namespace -f values.yaml --wait
            echo "✅ Elasticsearch deployed"
          else
            helm uninstall elasticsearch -n logging || true
            echo "🗑️ Elasticsearch removed"
          fi
      
      - name: Deploy Kibana
        if: |
          (github.event.inputs.tool == 'kibana' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd logging/kibana
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            echo "⏳ Waiting 60s for Elasticsearch to initialize..."
            sleep 60
            helm upgrade --install kibana . -n logging --create-namespace -f values.yaml --wait --timeout 5m
            echo "✅ Kibana deployed"
            kubectl get svc -n logging kibana
          else
            helm uninstall kibana -n logging || true
            echo "🗑️ Kibana removed"
          fi
      
      - name: Deploy Filebeat
        if: |
          (github.event.inputs.tool == 'filebeat' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd logging/filebeat
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            helm upgrade --install filebeat . -n logging --create-namespace -f values.yaml --wait
            echo "✅ Filebeat deployed"
          else
            helm uninstall filebeat -n logging || true
            echo "🗑️ Filebeat removed"
          fi
      
      - name: Deploy Nginx Ingress
        if: |
          (github.event.inputs.tool == 'nginx-ingress' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          cd ingress/nginx-ingress
          helm dependency update
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            helm upgrade --install nginx-ingress . -n ingress-nginx --create-namespace -f values.yaml --wait
            echo "✅ Nginx Ingress deployed"
            kubectl get svc -n ingress-nginx
          else
            helm uninstall nginx-ingress -n ingress-nginx || true
            echo "🗑️ Nginx Ingress removed"
          fi
      
      - name: Deploy ExternalDNS
        if: |
          (github.event.inputs.tool == 'external-dns' || github.event.inputs.tool == 'all') &&
          (github.event.inputs.action == 'install' || github.event.inputs.action == 'uninstall')
        continue-on-error: true
        run: |
          if [ "${{ github.event.inputs.action }}" == "install" ]; then
            echo "🚀 Installing ExternalDNS..."
            helm repo add external-dns https://kubernetes-sigs.github.io/external-dns/
            helm repo update
            cd dns/external-dns
            helm upgrade --install external-dns external-dns/external-dns \
              -n external-dns \
              -f values.yaml \
              --wait --timeout 5m
            echo "✅ ExternalDNS deployed"
            kubectl get pods -n external-dns
          else
            helm uninstall external-dns -n external-dns || true
            echo "🗑️ ExternalDNS removed"
          fi

  # Jenkins deploy with approval (only triggers for jenkins or all + install)
  deploy-jenkins:
    name: "Deploy Jenkins → Requires Approval"
    runs-on: ubuntu-latest
    environment: 
      name: jenkins-deploy-confirm  # Triggers the approval button
      url: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
    needs: [setup, deploy-non-jenkins]  # Wait for non-Jenkins to finish
    if: ${{ github.event.inputs.tool == 'jenkins' || github.event.inputs.tool == 'all' }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.EKS_CLUSTER_NAME }}
          kubectl get nodes
      
      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: 'latest'
      
      - name: Install eksctl
        run: |
          ARCH=amd64
          PLATFORM=$(uname -s)_$ARCH
          curl -sLO "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_$PLATFORM.tar.gz"
          tar -xzf eksctl_$PLATFORM.tar.gz -C /tmp && rm eksctl_$PLATFORM.tar.gz
          sudo mv /tmp/eksctl /usr/local/bin
          eksctl version
      
      - name: Deploy Jenkins (Post-Approval)
        if: github.event.inputs.action == 'install'
        run: |
          echo "✅ Approval confirmed — deploying Jenkins"
          echo "Run credentials script first: ./tools/cicd/jenkins/credentials.sh"
          cd cicd/jenkins
          helm dependency update
          helm upgrade --install jenkins . -n jenkins --create-namespace -f values.yaml --wait --timeout 10m
          kubectl apply -f jenkins-rbac.yaml
          echo "✅ Jenkins deployed with RBAC permissions"
          kubectl get svc -n jenkins jenkins
      
      - name: Uninstall Jenkins (No Approval Needed)
        if: github.event.inputs.action == 'uninstall'
        run: |
          echo "🗑️ Uninstalling Jenkins"
          cd cicd/jenkins
          helm uninstall jenkins -n jenkins || true
          kubectl delete -f jenkins-rbac.yaml || true
          echo "🗑️ Jenkins removed"

  # Summary job
  summary:
    name: Deployment Summary
    runs-on: ubuntu-latest
    needs: [deploy-non-jenkins, deploy-jenkins]
    if: always()
    steps:
      - name: Display Access Information
        if: github.event.inputs.action == 'install'
        run: |
          echo "========================================="
          echo "Deployment Complete!"
          echo "========================================="
          echo ""
          echo "📊 Access Grafana:"
          kubectl get svc -n monitoring grafana 2>/dev/null || echo "Not deployed"
          echo " Username: admin"
          echo " Password: admin"
          echo ""
          echo "📊 Access CloudWatch:"
          echo " Dashboard: https://console.aws.amazon.com/cloudwatch/home?region=${{ env.AWS_REGION }}#container-insights:infrastructure"
          echo ""
          echo "📊 Access Kibana:"
          kubectl get svc -n logging kibana 2>/dev/null || echo "Not deployed"
          echo ""
          echo "🌐 Ingress LoadBalancer:"
          kubectl get svc -n ingress-nginx 2>/dev/null || echo "Not deployed"
          echo ""
          echo "🚀 Access Jenkins:"
          kubectl get svc -n jenkins jenkins 2>/dev/null || echo "Not deployed"
          echo " Username: admin"
          echo " Password: admin (or from credentials script)"
          echo ""
          echo "💡 Tip: Use port-forward for localhost access:"
          echo " kubectl port-forward svc/grafana 3000:80 -n monitoring"
          echo " kubectl port-forward svc/jenkins 8080:8080 -n jenkins"
      - name: Uninstall Summary
        if: github.event.inputs.action == 'uninstall'
        run: |
          echo "========================================="
          echo "Uninstall Complete!"
          echo "========================================="
          echo "All selected tools have been removed."
